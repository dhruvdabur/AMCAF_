# tuning

OpenCV field-tuning panels and JSON tuning-file helpers.

`ellipse_static_tuning.py` and `straight_static_tuning.py` keep slider UI and tuning persistence separate from reusable controllers and from ROS transport code.
