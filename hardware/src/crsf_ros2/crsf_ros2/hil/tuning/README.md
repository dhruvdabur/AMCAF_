# tuning

OpenCV field-tuning panels and JSON tuning-file helpers.

`ellipse_static_tuning.py` keeps slider UI and tuning persistence separate from the reusable controller and from ROS transport code.
