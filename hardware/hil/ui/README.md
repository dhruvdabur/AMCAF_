# hil.ui

OpenCV preview and tuning-panel helpers.

| File | Purpose |
| --- | --- |
| `preview.py` | Preview resizing, status overlay text, labels, vectors, and no-op trackbar callback. |

Drawing primitives live here so the node can focus on what should be drawn,
while this package owns how it is rendered.
